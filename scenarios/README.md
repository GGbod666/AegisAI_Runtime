# 场景层规划

这一层不再使用过于抽象的 `plugins/` 命名，而是直接按问题场景组织。

原因很简单：

- 现在项目最重要的不是证明“我支持插件”
- 而是证明“我确实围绕三条主问题在搭系统”

每个场景目录都应该回答四件事：

- 解决什么问题
- 依赖哪些观测和标签
- 会触发哪些动作
- 如何验证收益

当前保留的三条主线：

- `ai_workload_awareness`
- `inference_tail_guard`
- `tool_call_booster`
